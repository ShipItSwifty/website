---
title: "API Reference"
description: "ShipItKit Swift API generated from DocC."
group: "API Reference"
order: 1
---

The **ShipItKit** Swift API is the embeddable library that powers the `shipit` CLI. Use it from your own Swift tools, CI scripts, or Swift Package Manager projects.

## Getting started

Add the dependency to your `Package.swift`:

```swift
dependencies: [
  .package(
    url: "https://github.com/ShipItSwifty/shipitswifty.git",
    branch: "main"
  )
]
```

## Browse the API

The full API reference is generated from DocC and rendered on this site:

[Open the ShipItKit API reference](/docs/api/shipitkit).
